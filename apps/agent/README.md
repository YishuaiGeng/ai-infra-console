# AI Infra Console Agent

The Agent collects host and NVIDIA GPU inventory and sends outbound registration and heartbeat requests to the Central API. It does not open an inbound management port or expose arbitrary command execution.

Phase 2 implementation and operating instructions are tracked in [`../../docs/phases/PHASE_2_AGENT.md`](../../docs/phases/PHASE_2_AGENT.md).
